[project]
name = "flower-power"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["flower-sim"]

[tool.uv.sources]
flower-sim = { workspace = true}

[tool.uv.workspace]
members = ["flower-sim"]
