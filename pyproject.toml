[project]
name = "flower-power"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["hydraulic-sim"]

[tool.uv.sources]
hydraulic-sim = { workspace = true}

[tool.uv.workspace]
members = ["hydraulic-sim"]
